services:
  kapowarr:
    container_name: kapowarr
    # Published images are linux/amd64-only; set KAPOWARR_IMAGE to a
    # digest-pinned reference for immutable production deployments.
    image: ${KAPOWARR_IMAGE:-noplexzone/kapowarr:latest}
    user: "99:100"
    environment:
      TZ: ${TZ:-Etc/UTC}
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=256m,mode=1777
    volumes:
      - kapowarr-db:/app/db
      - kapowarr-logs:/app/logs
      - /path/to/download_folder:/app/temp_downloads:rw
      - /path/to/comics:/comics:rw
      - /path/to/manga:/manga:rw
    ports:
      - "5656:5656"

volumes:
  kapowarr-db:
  kapowarr-logs:
