services:
  kapowarr:
    container_name: kapowarr
    image: ${KAPOWARR_IMAGE:-noplexzone/kapowarr:1.6.0}
    user: "99:100"
    environment:
      TZ: ${TZ:-Etc/UTC}
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=256m,mode=1777
    volumes:
      - kapowarr-db:/app/db
      - kapowarr-logs:/app/logs
      - /path/to/download_folder:/app/temp_downloads:rw
      - /path/to/comics:/comics:rw
      - /path/to/manga:/manga:rw
    ports:
      - "5656:5656"

volumes:
  kapowarr-db:
  kapowarr-logs:
